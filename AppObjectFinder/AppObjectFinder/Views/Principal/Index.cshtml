@model AppObjectFinder.Models.EntradaModels

@{
    ViewBag.Title = "Objetos Reportados";
}

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>3 Col Portfolio - Start Bootstrap Template</title>
    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="css/3-col-portfolio.css" rel="stylesheet">
</head>
<body>
    @using(Html.BeginForm())
    {
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">
                        Objetos Reportados
                        <small>@Model.ListaPrincipal.Count() objetos reportados</small>
                    </h1>
                </div>
            </div>
        </div>
    }

    <!-- Page Content -->
    <div class="container">
        <!-- Page Header -->
        <!-- /.row -->
        <!-- Projects Row -->

        <div class="container-fluid">
            <div class="row">
                <br />
                @foreach(var item in Model.ListaPrincipal)
                {
                    <div class="mix dogs col-sm-3">
                        <a href="#">

                            <img width="200" height="200" src='@Url.Action("GetImage", "Principal", new { idObjeto = item.P_idObjeto })' />

                            <h3>
                                <a href="#">@item.P_nombreObjeto</a>
                            </h3>

                            <h5>
                                @System.DateTime.Now.ToShortDateString()
                            </h5>
                            <section style="text-align:justify">
                                A través de esta plataforma puedes registrar tantos objetos que hayas encontrado. La finalidad de la plataforma es la administración de los objetos perdidos con el fin ayudar en lo posible aquella persona que haya extraviado el mismo.
                            </section>

                            <br />
                            <br />
                        </a>
                    </div>
                }

                @*@foreach(var item in Model.ListaPrincipal)
                    {
                        <div class="mix dogs col-sm-3">
                            <a href="#">

                                <img width="200" height="200" src='@Url.Action("GetImage", "Principal", new { idObjeto = item.P_idObjeto })' />

                                <h3>
                                    <a href="#">@item.P_nombreObjeto</a>
                                </h3>

                                <section style="text-align:justify">A través de esta plataforma puedes registrar tantos objetos que hayas encontrado. La finalidad de la plataforma es la administración de los objetos perdidos con el fin ayudar en lo posible aquella persona que haya extraviado el mismo.</section>
                                <br />
                                <br />
                            </a>
                        </div>
                    }

                    @foreach(var item in Model.ListaPrincipal)
                    {
                        <div class="mix dogs col-sm-3">
                            <a href="#">

                                <img width="200" height="200" src='@Url.Action("GetImage", "Principal", new { idObjeto = item.P_idObjeto })' />

                                <h3>
                                    <a href="#">@item.P_nombreObjeto</a>
                                </h3>

                                <section style="text-align:justify">A través de esta plataforma puedes registrar tantos objetos que hayas encontrado. La finalidad de la plataforma es la administración de los objetos perdidos con el fin ayudar en lo posible aquella persona que haya extraviado el mismo.</section>
                                <br />
                                <br />
                            </a>
                        </div>
                    }

                    @foreach(var item in Model.ListaPrincipal)
                    {
                        <div class="mix dogs col-sm-3">
                            <a href="#">

                                <img width="200" height="200" src='@Url.Action("GetImage", "Principal", new { idObjeto = item.P_idObjeto })' />

                                <h3>
                                    <a href="#">@item.P_nombreObjeto</a>
                                </h3>

                                <section style="text-align:justify">A través de esta plataforma puedes registrar tantos objetos que hayas encontrado. La finalidad de la plataforma es la administración de los objetos perdidos con el fin ayudar en lo posible aquella persona que haya extraviado el mismo.</section>

                                <br />
                                <br />
                            </a>
                        </div>
                    }*@
            </div>
        </div>
    </div>
    <!-- /.container -->
    <!-- jQuery -->
    <script src="js/jquery.js"></script>
    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>
</body>
